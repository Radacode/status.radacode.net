'use strict';

describe('rdcStatus.check module', function() {

  beforeEach(module('rdcStatus.check'));

  describe('check controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var checkCtrl = $controller('CheckCtrl');
      expect(checkCtrl).toBeDefined();
    }));

  });
});